rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Match any file in the bucket
    match /{allPaths=**} {
      // Allow read and write access only to authenticated users.
      // This is a secure default, preventing unauthorized access.
      allow read, write: if request.auth != null;
    }
  }
} 